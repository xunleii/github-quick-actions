name: Terraform

on:
  pull_request:
    paths: ["deployments/**.tf"]

jobs:
  terraform:
    name: IaaS - Terraform CI (for pull requests)
    uses: xunleii/github-actions-grimoire/.github/workflows/terraform.pull_requests.yaml@395ac9e7775736007e0726b2384c04cc9e1d9b68
